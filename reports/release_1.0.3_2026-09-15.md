# 1.0.3 导入耗时与结果提示

- 对隔离 MDR 样本测量：旧读取+解析 3.815 秒，新读取+解析 2.423 秒；解析结果完全一致。该结果不代表所有工作簿或电脑的耗时。
- 流式读取 Excel，仅构建非空单元格，保留值、数据类型、数字格式。避免为大量模板空白格式单元格构建常驻对象；原始 Excel 不变。
- 提交导入显示等待提示并禁用重复提交；完成后将结果区置顶并自动聚焦、滚动到结果。批次产品库/导出入口保留。
- 导入和导出预检错误为红色、警告为黄色、无问题为绿色，并显示数量；没有定时消失或自动切走。
- 浏览器实际上传隔离样本，确认导入 1 Basic / 1 UDI 后聚焦结果；实际 UDI_DI.PATCH 预检缺少版本号时，截图确认顶部红色错误卡片。
- compileall 和 118 项 unittest 全部通过（148.058 秒）。包含格式/类型保留、稀疏行、导入/导出界面、旧版迁移、原始制造商、XSD 等回归。
- 模板仍为 v2.13，XSD 仍为 3.0.30。未在 Windows 桌面或 EUDAMED Playground 进行交互验收。
